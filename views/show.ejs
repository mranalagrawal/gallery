<%- include('header')%>
    <%- include('nav')%>

        <div class="container bg-dark p-5 d-flex flex-wrap me-3 mb-3 ">
           
            
            <% cards.forEach(function(card){ %>

                <div class="card text-white me-3" style=" width: 17vmax;
                height: 22vmax;
                background-image: url('/uploads/<%= card.imagae %>');
                 background-size: cover;
                  background-position: center;
                background-repeat: no-repeat;">
           
                           <div class="card-body d-flex align-items-end">
                               <div class="card-text w-100 d-flex justify-content-between">
                                   <div>
                                       <h2><%= card.title%></h2>
                                       <p><%= card.author%></p>
                                   </div>
                                   <a class="btn btn-light" download="/uploads/<%= card.image%>" href="/uploads/<%= card.image%>"> <img height="40" src="/images/download.png"
                                           alt=""></a>
                               </div>
                           </div>
                       </div>

            <% }) %>

        </div>
    






        <%- include('footer')%>